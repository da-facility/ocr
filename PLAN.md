# Features

- quick, accurate, stable camera detection
- restorable sessions

# OCR Outputs
I want per session customizable OCR outputs. The output types that come to mind immediately are JSON, CSV, Plaintext; served via HTTP, sent via HTTP or saved to a local file